//数字排序
//num1乱序，num2顺序，n为数组长度
//记录比测试数小的数以及等于测试数并且排在测试数之后的数
    p=0;
	q=0; 
	for(i=0;i<n;i++){
		for(j=0;j<n;j++){
			if(num1[j]<num1[i])
			  p++;
			if(num1[j]==num1[i]){ 
			    q++;
			    if(j<=i)
			      q--;
			} 
		}
		num2[p+q]=num1[i]; 
		p=0;
		q=0; 
	}
